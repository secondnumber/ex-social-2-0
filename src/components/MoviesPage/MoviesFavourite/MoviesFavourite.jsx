import React from 'react';
import classes from './MoviesFavourite.module.scss';
import Movie from '../Movie/Movie';

const MoviesFavourite = (props) => {
  return (
    <ul className={classes.list}>
      F
    </ul>
  );
};
export default MoviesFavourite;
